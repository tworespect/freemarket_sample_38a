%figure
  .message-user-unknown
    %img{alt: "", src: "//www-mercari-jp.akamaized.net/assets/img/common/common/user-icon.svg?1016580576"}/
  - if comment.user_id == @product.user_id && comment.text != "出品者がコメントを削除しました"
    .product-exhibit-mark
      出品者
  - else
- unless comment.text == "出品者がコメントを削除しました"
  .message-name
    = comment.user.name
